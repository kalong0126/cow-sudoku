# 项目协作说明

## 基本功能与架构

- 项目名称为“小牛数独”，是面向手机、平板和桌面浏览器的纯静态 PWA 游戏。
- 使用原生 HTML、CSS 和 JavaScript，无前端构建流程、后端服务或数据库。
- 游戏规则：每行、每列和每种颜色区域均有且仅有一只小牛，小牛之间不能相邻。
- 支持难度选择、生命值、背景音乐、连续十局挑战、挑战进度恢复和历史统计。
- 十局挑战与音乐设置保存在当前浏览器的 localStorage；线上通过 api64.ipify.org 获取公网 IP，用于本浏览器历史记录筛选，不代表服务器汇总或跨设备同步。

## 文件职责

- `index.html`：页面结构、棋盘容器、挑战操作和统计弹窗。
- `styles.css`：界面样式、响应式布局与弹窗样式。
- `app.js`：CowSudokuCore 游戏核心，以及页面交互、音乐、挑战状态、计时和本地持久化逻辑。
- `sw.js`：Service Worker，同源 GET 资源缓存与离线访问。
- `manifest.webmanifest`、`icon.svg`：PWA 安装信息和图标。
- `.nojekyll`：静态托管辅助文件。
- `README.md`：本机运行方法及功能说明。

## 开发与验证

- 在项目目录运行 `python -m http.server 8080`，通过 `http://localhost:8080` 访问。
- Service Worker 需在浏览器允许的安全上下文运行；直接打开本地 HTML 或普通局域网 HTTP 不能保证离线缓存可用。
- 修改 JavaScript 后执行 `node --check app.js` 和 `node --check sw.js`；提交前执行 `git diff --check`。
- 修改页面行为后，按影响范围手动验证游戏操作、十局流程、刷新恢复和统计弹窗；语法检查不能代替浏览器功能验证。
- 修改缓存资源时，同步检查 `index.html` 的资源版本参数、`sw.js` 的 ASSETS 列表和 CACHE_NAME，避免浏览器继续使用旧资源。

## 协作规范

- 开始任务前先阅读本文件及相关项目文档；使用中文沟通。
- 使用 PowerShell Get-Content 时必须显式添加 `-Encoding UTF8`；新建和修改代码文件统一采用 UTF-8 无 BOM 编码。
- 不扫描 `node_modules`、`dist` 和 `target` 等依赖或构建产物目录。
- 保持纯静态架构，未明确要求时不引入后端、数据库或构建工具。
- 不将本机 IDE 工作区状态作为业务改动提交，不覆盖用户已有的本地修改。
- 如需批量删除文件，停止操作并请用户手动删除。
- 后续如新增后端，新增类和方法默认添加说明用途的中文注释，并按需记录关键环节日志。
